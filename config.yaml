theme: osuny
paginate: 18
enableRobotsTXT: false
removePathAccents: true
params:
  debug:
    active: true # todo change value

  # RENNES THEME CUSTOM
  custom:
    logo_link:
    logo_title:

  # DESIGN SYSTEM
  logo:
    header: "/assets/images/logo-black.svg"
    footer: "/assets/images/logo-white.svg"
  menu:
    primary:
      level_2:
        title:
          active: true
  breadcrumb:
    position: hero-start
  summary:
    position: hero
  search:
    active: false
  # We can also specify this options for each sharable Section
  share_links:
    # todo add copy link option
    enabled: true
    email: true
    facebook: true
    linkedin: true
    mastodon: false
    telegram: false
    twitter: false
    whatsapp: false

  # SECTIONS
  pages:
    single:
      taxonomies:
        position: hero

  posts:
    index:
      options:
        categories: true
    single:
      options:
        categories: true

  # BLOCKS
  # Todo

  # IMAGES SIZES
  image_sizes:
    blocks:
      call_to_action:
        mobile: 330
        tablet: 250
        desktop: 448